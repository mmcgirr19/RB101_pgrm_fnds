numbers = [1, 2, 3, 4]

numbers.delete_at(1)
# deletes the element at the specified index, returning that element or nil if out of range

numbers.delete(1)
# deletes all items from self that are equal to the object;
# returns the last deleted item or nil if no matching item found